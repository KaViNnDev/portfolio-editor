import { Typography } from '@mui/material';
import styled from 'styled-components';

export const AppenderFont = styled(Typography)`
  && {
    font-family: Inter;
    font-weight: 600;
    font-size: 14px;
    line-height: 24px;
    letter-spacing: -2%;
    color: #000000;
  }
`;
